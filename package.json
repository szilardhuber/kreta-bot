{
  "name": "kreta-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "rm -rf ekreta.zip && zip -r ekreta.zip * && terraform apply"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.145.0",
    "request": "^2.83.0"
  }
}
