{
  "name": "kreta-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "rm -rf ekreta.zip && zip -r ekreta.zip * && cd terraform && terraform apply && cd -"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.145.0",
    "moment": "^2.29.2",
    "request": "^2.83.0"
  }
}
